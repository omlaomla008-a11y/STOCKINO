import path from "path";

export const CONTENT_ROOT = path.join(process.cwd(), "src", "content");
export const HARDWARE_CONTENT_DIR = path.join(CONTENT_ROOT, "hardware");
export const BLOG_CONTENT_DIR = path.join(CONTENT_ROOT, "blog");
